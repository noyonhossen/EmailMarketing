@{
    ViewData["Title"] = "Index";
}

<!-- Page header -->
<div class="page-header">
    <div class="page-header-content header-elements-md-inline">
        <div class="page-title d-flex">
            <h4>Contact Upload list</h4>
        </div>
        <div class="header-elements d-none text-center text-md-left mb-3 mb-md-0">
            <a asp-controller="ContactUpload" asp-action="UploadContact" asp-area="Member" class="btn bg-primary"><i class="icon-file-upload2"></i> <span class="font-weight-semibold">Upload Contacts</span></a>
        </div>
    </div>
</div>
<!-- /page header -->
<!-- Content area -->
<div class="content pt-0">

    <!-- Basic datatable -->
    <div class="card">
        <div>
            <partial name="_ActionMessage" model="@Model.Response" />
        </div>

        <table class="table" id="contact-upload-table">
            <thead>
                <tr>
                    <th>File Name</th>
                    <th>Created</th>
                    <th>Is Send Email</th>
                    <th>Is Update Existing</th>
                    <th>Is Processing</th>
                    <th>Is Succeed</th>
                    <th>Succeed Entry</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody></tbody>
        </table>
    </div>
    <!-- /basic datatable -->
</div>

<!-- Danger modal -->
<div id="modal-delete" class="modal fade" tabindex="-1">
    <div class="modal-dialog modal-sm">
        <div class="modal-content">
            <div class="modal-header">
                <h6 class="modal-title">Delete</h6>
                <button type="button" class="close" data-dismiss="modal">&times;</button>
            </div>

            <div class="modal-body">
                <p>Are you sure want to delete?</p>
                <form id="deleteForm" action="@Url.Action("Delete", "ContactUpload", new { Area = "Member" })" method="post">
                    @Html.AntiForgeryToken()
                    <input type="hidden" id="deleteId" value="" name="id" />
                </form>
            </div>

            <div class="modal-footer">
                <button type="button" class="btn btn-link" data-dismiss="modal">Close</button>
                <button type="button" id="deleteButton" class="btn bg-danger">Yes, Delete!</button>
            </div>
        </div>
    </div>
</div>
<!-- /default modal -->

@section Scripts {
    <partial name="~/Views/Shared/_ValidationScriptsPartial.cshtml" />
    <partial name="~/Views/Bundle/_DatatableScriptsPartial.cshtml" />
    <script src="~/js/project/contact-upload.js"></script>
    <script src="~/js/ajax-call.js"></script>

    <script>
        $(function () {
            var url = '@Url.Action("GetContactUploads", "ContactUpload", new { Area = "Member" })';
            var editUrl = '@Url.Action("Edit", "ContactUpload", new { Area = "Member" })';
            loadDatatable(url, editUrl);

            $('#contact-upload-table').on('click', '.show-bs-modal', function (event) {
                var id = $(this).data("id");
                var modal = $("#modal-delete");
                modal.find('.modal-body p').text('Are you sure want to delete this record?')
                $("#deleteId").val(id);
                modal.modal('show');
            });

            $("#deleteButton").click(function () {
                $("#deleteForm").submit();
            });
        });
    </script>
}